<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Confirm Password</title>
    <link rel="stylesheet" href="/css/login.css">
</head>

<body>
    <% if(error.length>0) { %>
        <div id="error">
            <h1>
                <%= error %>
            </h1>
        </div>
        <% } %>
            <% if(message.length>0) { %>
                <div id="message">
                    <h1>
                        <%= message %>
                    </h1>
                </div>
                <% } %>
                    <div id="heading">
                        <header>Movie <span id="modify"><strong>HUB</strong></span></header>
                        <p>Share your movie collection with your friends</p>

                    </div>
                    <div id="form">
                        <div id="collect_data">
                            <form action="/changePassword" method="POST">
                                <input type="hidden" value="<%= csrfToken %>" name="_csrf">
                                <!--the name here is specific for the csrf token and therfore should not be changed-->
                                <div class="entry">
                                    <!--maa chudae leave this all ux to front end-->
                                    <input type="password" name="password" id="password" placeholder="Current password" style="margin-left:31%">
                                </div>
                                <input type="hidden" name="userId" id="userId" value="<%= user %>">
                                <div class="btn" style="margin-left:23%">
                                    <button type="submit">Change</button>
                                </div>
                            </form>
                        </div>

                    </div>
</body>

</html>